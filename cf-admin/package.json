{
  "name": "vibeit-admin",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute vibeit-briefs --local --file=schema.sql",
    "db:remote": "wrangler d1 execute vibeit-briefs --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.33.1"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
